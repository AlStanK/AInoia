# Локальна перевірка (див. README → Локальна перевірка наскрізь). Не для продакшну.
db-uri = "postgres://authenticator:localtest@127.0.0.1:5599/poll"
db-schemas = "diagnostic"
db-anon-role = "web_anon"
server-host = "127.0.0.1"
server-port = 3999
openapi-mode = "disabled"
